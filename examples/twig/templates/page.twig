{% macro page() %}
    {{ container_open(false, {'attributes': {'style': 'padding-top: 3rem;padding-bottom: 3rem;'}}) }}

    <div style="text-align:center;margin-bottom:3rem;">
        <h1>PHP UI Kit Examples: Twig</h1>
        <h2>Current CSS Framework: <strong>{{ current_framework }}</strong></h2>

        {% if current_framework == 'bootstrap5' %}
            <p><a href="/examples/twig/?sm">Open Fomantic UI version</a></p>
        {% else %}
            <p><a href="/examples/twig/">Open Bootstrap 5 version</a></p>
        {% endif %}

        <p>This file shows how to use components in Twig.</p>
        <a href="/examples/">Return to main page</a>
    </div>

    {{ accordion('example', {
        'Title 1': 'Content 1',
        'Title 2': 'Content 2',
    }) }}

    {{ container_close() }}
{% endmacro %}

{{ layout(_self.page(), {'title': 'PHP UI Kit Examples: Twig'}) }}

<!doctype html>
<html lang="{{ lang }}">
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    {{ head_tags|raw }}
    {{ css_codes|raw }}
</head>
<body{{ attributes|space|raw }}>
{{ body|raw }}
{{ footer_tags|raw }}
</body>
</html>

<div class="alert {{ color }}{{ class|space }}{{ dismiss ? ' alert-dismissible' : '' }}" role="alert"{{ attributes|space|raw }}>
    {{- text|raw -}}
    {%- if dismiss -%}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    {% endif %}
</div>
